version: 1

frontend:
  phases:
    build:
      commands:
        - echo "Building static site - no compilation required"
        - echo "Project: DocuMind AI - Document Intelligence Platform"

  artifacts:
    baseDirectory: ./
    files:
      - '**/*'
      - '**/.*'

  cache:
    paths: []
